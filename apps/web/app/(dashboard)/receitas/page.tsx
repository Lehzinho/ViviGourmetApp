"use client";

import { PageHeader } from "@/components/layout/PageHeader";

export default function ReceitasPage() {
  return (
    <PageHeader
      title="Receitas"
      subtitle="Listagem e editor de receitas com composição recursiva."
      actionLabel="Nova receita"
      onAction={() => {}}
    />
  );
}
