"use client";

import { PageHeader } from "@/components/layout/PageHeader";

export default function CardapioPage() {
  return (
    <PageHeader
      title="Cardápio público"
      subtitle="Configure o cardápio, visibilidade e ordem dos itens."
      actionLabel="Novo cardápio"
      onAction={() => {}}
    />
  );
}
