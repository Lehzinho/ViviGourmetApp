"use client";

import { PageHeader } from "@/components/layout/PageHeader";

export default function DespesasPage() {
  return (
    <PageHeader
      title="Despesas"
      subtitle="Custos fixos e variáveis por período."
      actionLabel="Nova despesa"
      onAction={() => {}}
    />
  );
}
